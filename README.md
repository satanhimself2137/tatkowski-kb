# Tatkowski KB — Master Copy

This is the master copy of `tatkowski_knowledge_base.md`.

- **Read/written directly by Claude (Desktop Commander) in desktop sessions**
- **Project copy at `/mnt/project/tatkowski_knowledge_base_v5.md` auto-loads context** — refreshed on weekly cadence or after major updates
- Git-tracked for version history and rollback

## Workflow
1. Desktop sessions: Claude reads + writes `tatkowski_knowledge_base.md` here directly — no download/reupload needed
2. After significant updates: replace the project file attachment manually to keep mobile sessions in sync
3. Commit after each session update: `git add -A && git commit -m "KB update — [date]"`
