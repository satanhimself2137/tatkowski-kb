# gsc-tools

Source code for the GSC weekly data pipeline. Lives in the KB repo as a versioned
backup and shared reference; the **executing copy** runs from `D:\tatkowski-gsc\`
on Maciej's machine (where node_modules, data/, logs/, and .env stay local).

When code here changes:
1. Edit in `D:\tatkowski-gsc\` (working copy)
2. Mirror the edit to this directory in the KB repo
3. Commit + push

For pipeline behaviour and bug history, see KB section "GSC data pipeline".
